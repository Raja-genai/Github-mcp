[project]
name = "github_mcp"
version = "0.1.0"
description = "An MCP which helps with GITHUB tasks with an agent"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=3.4.2",
]


